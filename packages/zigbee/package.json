{
  "name": "@homy/zigbee",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "test": "jest --detectOpenHandles"
  },
  "jest": {
    "preset": "@homy/jest-config"
  },
  "devDependencies": {
    "@homy/tsconfig": "workspace:*",
    "@homy/jest-config": "workspace:*",
    "@types/jest": "^28.1.6",
    "@types/node": "^18.0.6",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.4"
  }
}
